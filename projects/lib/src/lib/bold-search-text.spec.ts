import { BoldSearchText } from './bold-search-text';

describe('BoldSearchText', () => {
  it('should create an instance', () => {
    const directive = new BoldSearchText();
    expect(directive).toBeTruthy();
  });
});
